#!/usr/bin/perl
#get the first name from the user 
$fname=

#get the last name from the user 
$lname=

# Apply chomp on fname 
chomp($fname);

# Apply another time chomp on fname and print the value


# Apply another time chomp on fname and print the value 



# What is the difference between chop and chomp ? 