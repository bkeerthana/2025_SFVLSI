#!/usr/bin/perl
#get the first name from the user 
$fname=

#get the last name from the user 
$lname=

# perform chomp on fname 
chomp($fname);

#perform chomp on lname


#concat fname and lname and store in name
$name=

#print the value of name
Print name;
